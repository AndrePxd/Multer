h1 Formulario Envío Imágenes
div 
form(action="/upload", method="POST",enctype="multipart/form-data")
    label(for="Imagen")
    input(type="file",name="imagen")
    input(type="submit",value="Enviar") 